node_modules
dist
build
.DS_Store

# Env (jamais de secrets versionnés)
.env
.env.local
.env.*.local

# Serveur / uploads (vivent sur le VPS)
server/public
public/uploads/

# Images sources brutes (on ne versionne que les .webp optimisés générés)
client/public/images/_raw/

# Dumps DB (ne jamais versionner — contiennent des données)
dump.sql
dump-data.sql
*.dump
migrations/legacy_import.sql

# Divers plateformes
.replit
replit.md
.local/

# Métadonnées d'outillage
.claude/
vite.config.ts.*
*.tar.gz
/leaguebs_deploy